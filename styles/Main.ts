import { StyleSheet } from "react-native";

const Main = StyleSheet.create({
    main: {
        backgroundColor: "#ECECEC",
        flex: 1,
        paddingTop: "6%",
    },
});

export default Main;
